<Window x:Class="ChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatClient"
        mc:Ignorable="d"
        Title="Client" Height="350" Width="400">
    
    <Window.Resources>
        <Style x:Key="EmojiButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Tên: " VerticalAlignment="Center"/>
            <TextBox x:Name="txtUserName" Text="User1" Width="80" Margin="0,0,5,0"/>
            
            <TextBox x:Name="txtIP" Text="127.0.0.1" Width="100" Margin="0,0,5,0"/>
            <Button x:Name="btnConnect" Content="Connect" Click="btnConnect_Click" Width="80"/>
        </StackPanel>

        <ListBox x:Name="lstChat" Grid.Row="1" Margin="0,0,0,10" FontFamily="Segoe UI Emoji"/>

        <Grid Grid.Row="2">
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="txtMsg" Grid.Column="0" Margin="0,0,5,0" VerticalContentAlignment="Center"  KeyDown="txtMsg_KeyDown"/>
            
            <Button x:Name="btnEmoji" Content="😀" Grid.Column="1" Width="40" Margin="0,0,5,0" Click="btnEmoji_Click"/>
            
            <Button x:Name="btnSend" Content="Send" Click="btnSend_Click" Grid.Column="2" Width="60"/>

            <Popup x:Name="popEmoji" PlacementTarget="{Binding ElementName=btnEmoji}" Placement="Top" StaysOpen="False">
                <Border Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="5">
                    <UniformGrid Columns="4">
                        <Button Content="😀" Style="{StaticResource EmojiButtonStyle}" Click="AddEmoji_Click"/>
                        <Button Content="😂" Style="{StaticResource EmojiButtonStyle}" Click="AddEmoji_Click"/>
                        <Button Content="😍" Style="{StaticResource EmojiButtonStyle}" Click="AddEmoji_Click"/>
                        <Button Content="👍" Style="{StaticResource EmojiButtonStyle}" Click="AddEmoji_Click"/>
                        <Button Content="🔥" Style="{StaticResource EmojiButtonStyle}" Click="AddEmoji_Click"/>
                        <Button Content="🎉" Style="{StaticResource EmojiButtonStyle}" Click="AddEmoji_Click"/>
                        <Button Content="❤️" Style="{StaticResource EmojiButtonStyle}" Click="AddEmoji_Click"/>
                        <Button Content="😭" Style="{StaticResource EmojiButtonStyle}" Click="AddEmoji_Click"/>
                    </UniformGrid>
                </Border>
            </Popup>
            
        </Grid>

    </Grid>
</Window>
